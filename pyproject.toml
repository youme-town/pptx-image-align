[project]
name = "pptx-image-align"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pillow>=12.0.0",
    "python-pptx>=1.0.2",
    "pyyaml>=6.0.3",
]
